<!DOCTYPE html>
<html>
  <head>
    <title>記事投稿アプリ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="navbar">
      <div class="container">
        <%= link_to "記事投稿アプリ", root_path, class: "navbar-brand" %>
        <nav class="navbar-nav">
          <% if user_signed_in? %>
            <%= link_to "新規投稿", new_article_path, class: "nav-link" %>
            <%= link_to "ログアウト", destroy_user_session_path, data: { "turbo-method": :delete }, class: "nav-link" %>
            <span class="nav-text">ようこそ、<%= current_user.email %>さん</span>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "nav-link" %>
            <%= link_to "新規登録", new_user_registration_path, class: "nav-link" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="container">
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>
